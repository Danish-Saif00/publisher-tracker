{
  "name": "@affiliate-tracker/runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Single Render web-service supervisor for the API, tracker, and worker runtimes.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --enable-source-maps dist/main.js",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@affiliate-tracker/config": "workspace:*",
    "@affiliate-tracker/observability": "workspace:*"
  }
}
